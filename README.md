1) run in the terminal "npm install express --save" to install express server
2)run both commands in the terminal to install sqlite and sqlite3
"npm install sqlite --save"
"npm install sqlite3 --save"

